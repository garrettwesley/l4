using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ExitAtomLesson : MonoBehaviour
{
/*
    public Button Exit_button;
    Camera FPSCam;
    Camera TeachCam;
    private bool ExitTime = false;

    private void Start()
    {
        FPSCam.GetComponent<Camera>.enabled = true;
        TeachCam.GetComponent<Camera>.enabled = false;

        void OnTriggerEnter(Collider FPSController) {
            FPSCam.GetComponent<Camera>.enabled = false;
            TeachCam.GetComponent<Camera>.enabled = true;

            if (Input.GetMouseButton)
            {
                
            }
        }

       
    } */
}


